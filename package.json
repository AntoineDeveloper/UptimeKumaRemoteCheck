{
  "name": "uptime-kuma-remote-check",
  "version": "1.0.0",
  "description": "A small, secure relay that lets a cloud-hosted Uptime Kuma run HTTP(S), TCP and ping checks against hosts inside a private network. Exposed publicly, locked down with a header secret and an allowlist.",
  "main": "src/server.js",
  "scripts": {
    "setup": "node scripts/setup.js",
    "start": "node src/server.js",
    "dev": "node src/server.js"
  },
  "keywords": [
    "uptime-kuma",
    "monitoring",
    "relay",
    "proxy",
    "ping",
    "tcp",
    "http",
    "self-hosted"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
